# npm configuration for FIT Viewer extension

# Use exact versions when installing
save-exact=true

# Automatically save dependencies when installing
save=true

# Set registry (optional - use default)
# registry=https://registry.npmjs.org/

# Production install only (for CI/CD)
# production=false

# Logging level
loglevel=warn

# Fund messages
fund=false
